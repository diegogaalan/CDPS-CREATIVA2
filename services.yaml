apiVersion: v1
kind: Service
metadata:
  name: details
  namespace: cdps-3
  labels:
    app: details
    service: details
spec:
  ports:
  - port: 7070
    targetPort: 7070
    name: http
  selector:
    app: details
---
apiVersion: v1
kind: Service
metadata:
  name: ratings
  namespace: cdps-3
  labels:
    app: ratings
    service: ratings
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: ratings
---
apiVersion: v1
kind: Service
metadata:
  name: reviews
  namespace: cdps-3
  labels:
    app: reviews
    service: reviews
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: reviews
---
apiVersion: v1
kind: Service
metadata:
  name: productpage
  namespace: cdps-3
  labels:
    app: productpage
    service: productpage
spec:
  type: LoadBalancer 
  ports:
  - port: 9080
    name: http
  selector:
    app: productpage